  (*---------------------------------------------------------
   * Autogenerated -- do not modify
   * Generated from 
   * /home/luca/Datos/open62541/open62541-pack-1.1/tools/schema/StatusCode.csv 
   * with script
   * ./generate_statuscode_fpc.py
   *-------------------------------------------------------*)

  (**
   * .. _statuscodes:
   *
   * StatusCodes
   * -----------
   * StatusCodes are extensively used in the OPC UA protocol and in the open62541
   * API. They are represented by the :ref:`statuscode` data type. The following
   * definitions are autogenerated from the ``Opc.Ua.StatusCodes.csv`` file provided
   * with the OPC UA standard. *)

  (* These StatusCodes are manually generated. *)
  UA_STATUSCODE_GOOD = $00;
  UA_STATUSCODE_INFOTYPE_DATAVALUE = $00000400;
  UA_STATUSCODE_INFOBITS_OVERFLOW = $00000080;

  UA_STATUSCODE_BADUNEXPECTEDERROR = $80010000; (* An unexpected error occurred. *)
  UA_STATUSCODE_BADINTERNALERROR = $80020000; (* An internal error occurred as a result of a programming or configuration error. *)
  UA_STATUSCODE_BADOUTOFMEMORY = $80030000; (* Not enough memory to complete the operation. *)
  UA_STATUSCODE_BADRESOURCEUNAVAILABLE = $80040000; (* An operating system resource is not available. *)
  UA_STATUSCODE_BADCOMMUNICATIONERROR = $80050000; (* A low level communication error occurred. *)
  UA_STATUSCODE_BADENCODINGERROR = $80060000; (* Encoding halted because of invalid data in the objects being serialized. *)
  UA_STATUSCODE_BADDECODINGERROR = $80070000; (* Decoding halted because of invalid data in the stream. *)
  UA_STATUSCODE_BADENCODINGLIMITSEXCEEDED = $80080000; (* The message encoding/decoding limits imposed by the stack have been exceeded. *)
  UA_STATUSCODE_BADREQUESTTOOLARGE = $80B80000; (* The request message size exceeds limits set by the server. *)
  UA_STATUSCODE_BADRESPONSETOOLARGE = $80B90000; (* The response message size exceeds limits set by the client. *)
  UA_STATUSCODE_BADUNKNOWNRESPONSE = $80090000; (* An unrecognized response was received from the server. *)
  UA_STATUSCODE_BADTIMEOUT = $800A0000; (* The operation timed out. *)
  UA_STATUSCODE_BADSERVICEUNSUPPORTED = $800B0000; (* The server does not support the requested service. *)
  UA_STATUSCODE_BADSHUTDOWN = $800C0000; (* The operation was cancelled because the application is shutting down. *)
  UA_STATUSCODE_BADSERVERNOTCONNECTED = $800D0000; (* The operation could not complete because the client is not connected to the server. *)
  UA_STATUSCODE_BADSERVERHALTED = $800E0000; (* The server has stopped and cannot process any requests. *)
  UA_STATUSCODE_BADNOTHINGTODO = $800F0000; (* There was nothing to do because the client passed a list of operations with no elements. *)
  UA_STATUSCODE_BADTOOMANYOPERATIONS = $80100000; (* The request could not be processed because it specified too many operations. *)
  UA_STATUSCODE_BADTOOMANYMONITOREDITEMS = $80DB0000; (* The request could not be processed because there are too many monitored items in the subscription. *)
  UA_STATUSCODE_BADDATATYPEIDUNKNOWN = $80110000; (* The extension object cannot be (de)serialized because the data type id is not recognized. *)
  UA_STATUSCODE_BADCERTIFICATEINVALID = $80120000; (* The certificate provided as a parameter is not valid. *)
  UA_STATUSCODE_BADSECURITYCHECKSFAILED = $80130000; (* An error occurred verifying security. *)
  UA_STATUSCODE_BADCERTIFICATEPOLICYCHECKFAILED = $81140000; (* The certificate does not meet the requirements of the security policy. *)
  UA_STATUSCODE_BADCERTIFICATETIMEINVALID = $80140000; (* The certificate has expired or is not yet valid. *)
  UA_STATUSCODE_BADCERTIFICATEISSUERTIMEINVALID = $80150000; (* An issuer certificate has expired or is not yet valid. *)
  UA_STATUSCODE_BADCERTIFICATEHOSTNAMEINVALID = $80160000; (* The HostName used to connect to a server does not match a HostName in the certificate. *)
  UA_STATUSCODE_BADCERTIFICATEURIINVALID = $80170000; (* The URI specified in the ApplicationDescription does not match the URI in the certificate. *)
  UA_STATUSCODE_BADCERTIFICATEUSENOTALLOWED = $80180000; (* The certificate may not be used for the requested operation. *)
  UA_STATUSCODE_BADCERTIFICATEISSUERUSENOTALLOWED = $80190000; (* The issuer certificate may not be used for the requested operation. *)
  UA_STATUSCODE_BADCERTIFICATEUNTRUSTED = $801A0000; (* The certificate is not trusted. *)
  UA_STATUSCODE_BADCERTIFICATEREVOCATIONUNKNOWN = $801B0000; (* It was not possible to determine if the certificate has been revoked. *)
  UA_STATUSCODE_BADCERTIFICATEISSUERREVOCATIONUNKNOWN = $801C0000; (* It was not possible to determine if the issuer certificate has been revoked. *)
  UA_STATUSCODE_BADCERTIFICATEREVOKED = $801D0000; (* The certificate has been revoked. *)
  UA_STATUSCODE_BADCERTIFICATEISSUERREVOKED = $801E0000; (* The issuer certificate has been revoked. *)
  UA_STATUSCODE_BADCERTIFICATECHAININCOMPLETE = $810D0000; (* The certificate chain is incomplete. *)
  UA_STATUSCODE_BADUSERACCESSDENIED = $801F0000; (* User does not have permission to perform the requested operation. *)
  UA_STATUSCODE_BADIDENTITYTOKENINVALID = $80200000; (* The user identity token is not valid. *)
  UA_STATUSCODE_BADIDENTITYTOKENREJECTED = $80210000; (* The user identity token is valid but the server has rejected it. *)
  UA_STATUSCODE_BADSECURECHANNELIDINVALID = $80220000; (* The specified secure channel is no longer valid. *)
  UA_STATUSCODE_BADINVALIDTIMESTAMP = $80230000; (* The timestamp is outside the range allowed by the server. *)
  UA_STATUSCODE_BADNONCEINVALID = $80240000; (* The nonce does appear to be not a random value or it is not the correct length. *)
  UA_STATUSCODE_BADSESSIONIDINVALID = $80250000; (* The session id is not valid. *)
  UA_STATUSCODE_BADSESSIONCLOSED = $80260000; (* The session was closed by the client. *)
  UA_STATUSCODE_BADSESSIONNOTACTIVATED = $80270000; (* The session cannot be used because ActivateSession has not been called. *)
  UA_STATUSCODE_BADSUBSCRIPTIONIDINVALID = $80280000; (* The subscription id is not valid. *)
  UA_STATUSCODE_BADREQUESTHEADERINVALID = $802A0000; (* The header for the request is missing or invalid. *)
  UA_STATUSCODE_BADTIMESTAMPSTORETURNINVALID = $802B0000; (* The timestamps to return parameter is invalid. *)
  UA_STATUSCODE_BADREQUESTCANCELLEDBYCLIENT = $802C0000; (* The request was cancelled by the client. *)
  UA_STATUSCODE_BADTOOMANYARGUMENTS = $80E50000; (* Too many arguments were provided. *)
  UA_STATUSCODE_BADLICENSEEXPIRED = $810E0000; (* The server requires a license to operate in general or to perform a service or operation *)
  UA_STATUSCODE_BADLICENSELIMITSEXCEEDED = $810F0000; (* The server has limits on number of allowed operations / objects *)
  UA_STATUSCODE_BADLICENSENOTAVAILABLE = $81100000; (* The server does not have a license which is required to operate in general or to perform a service or operation. *)
  UA_STATUSCODE_GOODSUBSCRIPTIONTRANSFERRED = $002D0000; (* The subscription was transferred to another session. *)
  UA_STATUSCODE_GOODCOMPLETESASYNCHRONOUSLY = $002E0000; (* The processing will complete asynchronously. *)
  UA_STATUSCODE_GOODOVERLOAD = $002F0000; (* Sampling has slowed down due to resource limitations. *)
  UA_STATUSCODE_GOODCLAMPED = $00300000; (* The value written was accepted but was clamped. *)
  UA_STATUSCODE_BADNOCOMMUNICATION = $80310000; (* Communication with the data source is defined *)
  UA_STATUSCODE_BADWAITINGFORINITIALDATA = $80320000; (* Waiting for the server to obtain values from the underlying data source. *)
  UA_STATUSCODE_BADNODEIDINVALID = $80330000; (* The syntax of the node id is not valid. *)
  UA_STATUSCODE_BADNODEIDUNKNOWN = $80340000; (* The node id refers to a node that does not exist in the server address space. *)
  UA_STATUSCODE_BADATTRIBUTEIDINVALID = $80350000; (* The attribute is not supported for the specified Node. *)
  UA_STATUSCODE_BADINDEXRANGEINVALID = $80360000; (* The syntax of the index range parameter is invalid. *)
  UA_STATUSCODE_BADINDEXRANGENODATA = $80370000; (* No data exists within the range of indexes specified. *)
  UA_STATUSCODE_BADDATAENCODINGINVALID = $80380000; (* The data encoding is invalid. *)
  UA_STATUSCODE_BADDATAENCODINGUNSUPPORTED = $80390000; (* The server does not support the requested data encoding for the node. *)
  UA_STATUSCODE_BADNOTREADABLE = $803A0000; (* The access level does not allow reading or subscribing to the Node. *)
  UA_STATUSCODE_BADNOTWRITABLE = $803B0000; (* The access level does not allow writing to the Node. *)
  UA_STATUSCODE_BADOUTOFRANGE = $803C0000; (* The value was out of range. *)
  UA_STATUSCODE_BADNOTSUPPORTED = $803D0000; (* The requested operation is not supported. *)
  UA_STATUSCODE_BADNOTFOUND = $803E0000; (* A requested item was not found or a search operation ended without success. *)
  UA_STATUSCODE_BADOBJECTDELETED = $803F0000; (* The object cannot be used because it has been deleted. *)
  UA_STATUSCODE_BADNOTIMPLEMENTED = $80400000; (* Requested operation is not implemented. *)
  UA_STATUSCODE_BADMONITORINGMODEINVALID = $80410000; (* The monitoring mode is invalid. *)
  UA_STATUSCODE_BADMONITOREDITEMIDINVALID = $80420000; (* The monitoring item id does not refer to a valid monitored item. *)
  UA_STATUSCODE_BADMONITOREDITEMFILTERINVALID = $80430000; (* The monitored item filter parameter is not valid. *)
  UA_STATUSCODE_BADMONITOREDITEMFILTERUNSUPPORTED = $80440000; (* The server does not support the requested monitored item filter. *)
  UA_STATUSCODE_BADFILTERNOTALLOWED = $80450000; (* A monitoring filter cannot be used in combination with the attribute specified. *)
  UA_STATUSCODE_BADSTRUCTUREMISSING = $80460000; (* A mandatory structured parameter was missing or null. *)
  UA_STATUSCODE_BADEVENTFILTERINVALID = $80470000; (* The event filter is not valid. *)
  UA_STATUSCODE_BADCONTENTFILTERINVALID = $80480000; (* The content filter is not valid. *)
  UA_STATUSCODE_BADFILTEROPERATORINVALID = $80C10000; (* An unrecognized operator was provided in a filter. *)
  UA_STATUSCODE_BADFILTEROPERATORUNSUPPORTED = $80C20000; (* A valid operator was provided *)
  UA_STATUSCODE_BADFILTEROPERANDCOUNTMISMATCH = $80C30000; (* The number of operands provided for the filter operator was less then expected for the operand provided. *)
  UA_STATUSCODE_BADFILTEROPERANDINVALID = $80490000; (* The operand used in a content filter is not valid. *)
  UA_STATUSCODE_BADFILTERELEMENTINVALID = $80C40000; (* The referenced element is not a valid element in the content filter. *)
  UA_STATUSCODE_BADFILTERLITERALINVALID = $80C50000; (* The referenced literal is not a valid value. *)
  UA_STATUSCODE_BADCONTINUATIONPOINTINVALID = $804A0000; (* The continuation point provide is longer valid. *)
  UA_STATUSCODE_BADNOCONTINUATIONPOINTS = $804B0000; (* The operation could not be processed because all continuation points have been allocated. *)
  UA_STATUSCODE_BADREFERENCETYPEIDINVALID = $804C0000; (* The reference type id does not refer to a valid reference type node. *)
  UA_STATUSCODE_BADBROWSEDIRECTIONINVALID = $804D0000; (* The browse direction is not valid. *)
  UA_STATUSCODE_BADNODENOTINVIEW = $804E0000; (* The node is not part of the view. *)
  UA_STATUSCODE_BADNUMERICOVERFLOW = $81120000; (* The number was not accepted because of a numeric overflow. *)
  UA_STATUSCODE_BADSERVERURIINVALID = $804F0000; (* The ServerUri is not a valid URI. *)
  UA_STATUSCODE_BADSERVERNAMEMISSING = $80500000; (* No ServerName was specified. *)
  UA_STATUSCODE_BADDISCOVERYURLMISSING = $80510000; (* No DiscoveryUrl was specified. *)
  UA_STATUSCODE_BADSEMPAHOREFILEMISSING = $80520000; (* The semaphore file specified by the client is not valid. *)
  UA_STATUSCODE_BADREQUESTTYPEINVALID = $80530000; (* The security token request type is not valid. *)
  UA_STATUSCODE_BADSECURITYMODEREJECTED = $80540000; (* The security mode does not meet the requirements set by the server. *)
  UA_STATUSCODE_BADSECURITYPOLICYREJECTED = $80550000; (* The security policy does not meet the requirements set by the server. *)
  UA_STATUSCODE_BADTOOMANYSESSIONS = $80560000; (* The server has reached its maximum number of sessions. *)
  UA_STATUSCODE_BADUSERSIGNATUREINVALID = $80570000; (* The user token signature is missing or invalid. *)
  UA_STATUSCODE_BADAPPLICATIONSIGNATUREINVALID = $80580000; (* The signature generated with the client certificate is missing or invalid. *)
  UA_STATUSCODE_BADNOVALIDCERTIFICATES = $80590000; (* The client did not provide at least one software certificate that is valid and meets the profile requirements for the server. *)
  UA_STATUSCODE_BADIDENTITYCHANGENOTSUPPORTED = $80C60000; (* The server does not support changing the user identity assigned to the session. *)
  UA_STATUSCODE_BADREQUESTCANCELLEDBYREQUEST = $805A0000; (* The request was cancelled by the client with the Cancel service. *)
  UA_STATUSCODE_BADPARENTNODEIDINVALID = $805B0000; (* The parent node id does not to refer to a valid node. *)
  UA_STATUSCODE_BADREFERENCENOTALLOWED = $805C0000; (* The reference could not be created because it violates constraints imposed by the data model. *)
  UA_STATUSCODE_BADNODEIDREJECTED = $805D0000; (* The requested node id was reject because it was either invalid or server does not allow node ids to be specified by the client. *)
  UA_STATUSCODE_BADNODEIDEXISTS = $805E0000; (* The requested node id is already used by another node. *)
  UA_STATUSCODE_BADNODECLASSINVALID = $805F0000; (* The node class is not valid. *)
  UA_STATUSCODE_BADBROWSENAMEINVALID = $80600000; (* The browse name is invalid. *)
  UA_STATUSCODE_BADBROWSENAMEDUPLICATED = $80610000; (* The browse name is not unique among nodes that share the same relationship with the parent. *)
  UA_STATUSCODE_BADNODEATTRIBUTESINVALID = $80620000; (* The node attributes are not valid for the node class. *)
  UA_STATUSCODE_BADTYPEDEFINITIONINVALID = $80630000; (* The type definition node id does not reference an appropriate type node. *)
  UA_STATUSCODE_BADSOURCENODEIDINVALID = $80640000; (* The source node id does not reference a valid node. *)
  UA_STATUSCODE_BADTARGETNODEIDINVALID = $80650000; (* The target node id does not reference a valid node. *)
  UA_STATUSCODE_BADDUPLICATEREFERENCENOTALLOWED = $80660000; (* The reference type between the nodes is already defined. *)
  UA_STATUSCODE_BADINVALIDSELFREFERENCE = $80670000; (* The server does not allow this type of self reference on this node. *)
  UA_STATUSCODE_BADREFERENCELOCALONLY = $80680000; (* The reference type is not valid for a reference to a remote server. *)
  UA_STATUSCODE_BADNODELETERIGHTS = $80690000; (* The server will not allow the node to be deleted. *)
  UA_STATUSCODE_UNCERTAINREFERENCENOTDELETED = $40BC0000; (* The server was not able to delete all target references. *)
  UA_STATUSCODE_BADSERVERINDEXINVALID = $806A0000; (* The server index is not valid. *)
  UA_STATUSCODE_BADVIEWIDUNKNOWN = $806B0000; (* The view id does not refer to a valid view node. *)
  UA_STATUSCODE_BADVIEWTIMESTAMPINVALID = $80C90000; (* The view timestamp is not available or not supported. *)
  UA_STATUSCODE_BADVIEWPARAMETERMISMATCH = $80CA0000; (* The view parameters are not consistent with each other. *)
  UA_STATUSCODE_BADVIEWVERSIONINVALID = $80CB0000; (* The view version is not available or not supported. *)
  UA_STATUSCODE_UNCERTAINNOTALLNODESAVAILABLE = $40C00000; (* The list of references may not be complete because the underlying system is not available. *)
  UA_STATUSCODE_GOODRESULTSMAYBEINCOMPLETE = $00BA0000; (* The server should have followed a reference to a node in a remote server but did not. The result set may be incomplete. *)
  UA_STATUSCODE_BADNOTTYPEDEFINITION = $80C80000; (* The provided Nodeid was not a type definition nodeid. *)
  UA_STATUSCODE_UNCERTAINREFERENCEOUTOFSERVER = $406C0000; (* One of the references to follow in the relative path references to a node in the address space in another server. *)
  UA_STATUSCODE_BADTOOMANYMATCHES = $806D0000; (* The requested operation has too many matches to return. *)
  UA_STATUSCODE_BADQUERYTOOCOMPLEX = $806E0000; (* The requested operation requires too many resources in the server. *)
  UA_STATUSCODE_BADNOMATCH = $806F0000; (* The requested operation has no match to return. *)
  UA_STATUSCODE_BADMAXAGEINVALID = $80700000; (* The max age parameter is invalid. *)
  UA_STATUSCODE_BADSECURITYMODEINSUFFICIENT = $80E60000; (* The operation is not permitted over the current secure channel. *)
  UA_STATUSCODE_BADHISTORYOPERATIONINVALID = $80710000; (* The history details parameter is not valid. *)
  UA_STATUSCODE_BADHISTORYOPERATIONUNSUPPORTED = $80720000; (* The server does not support the requested operation. *)
  UA_STATUSCODE_BADINVALIDTIMESTAMPARGUMENT = $80BD0000; (* The defined timestamp to return was invalid. *)
  UA_STATUSCODE_BADWRITENOTSUPPORTED = $80730000; (* The server does not support writing the combination of value *)
  UA_STATUSCODE_BADTYPEMISMATCH = $80740000; (* The value supplied for the attribute is not of the same type as the attribute's value. *)
  UA_STATUSCODE_BADMETHODINVALID = $80750000; (* The method id does not refer to a method for the specified object. *)
  UA_STATUSCODE_BADARGUMENTSMISSING = $80760000; (* The client did not specify all of the input arguments for the method. *)
  UA_STATUSCODE_BADNOTEXECUTABLE = $81110000; (* The executable attribute does not allow the execution of the method. *)
  UA_STATUSCODE_BADTOOMANYSUBSCRIPTIONS = $80770000; (* The server has reached its maximum number of subscriptions. *)
  UA_STATUSCODE_BADTOOMANYPUBLISHREQUESTS = $80780000; (* The server has reached the maximum number of queued publish requests. *)
  UA_STATUSCODE_BADNOSUBSCRIPTION = $80790000; (* There is no subscription available for this session. *)
  UA_STATUSCODE_BADSEQUENCENUMBERUNKNOWN = $807A0000; (* The sequence number is unknown to the server. *)
  UA_STATUSCODE_BADMESSAGENOTAVAILABLE = $807B0000; (* The requested notification message is no longer available. *)
  UA_STATUSCODE_BADINSUFFICIENTCLIENTPROFILE = $807C0000; (* The client of the current session does not support one or more Profiles that are necessary for the subscription. *)
  UA_STATUSCODE_BADSTATENOTACTIVE = $80BF0000; (* The sub-state machine is not currently active. *)
  UA_STATUSCODE_BADALREADYEXISTS = $81150000; (* An equivalent rule already exists. *)
  UA_STATUSCODE_BADTCPSERVERTOOBUSY = $807D0000; (* The server cannot process the request because it is too busy. *)
  UA_STATUSCODE_BADTCPMESSAGETYPEINVALID = $807E0000; (* The type of the message specified in the header invalid. *)
  UA_STATUSCODE_BADTCPSECURECHANNELUNKNOWN = $807F0000; (* The SecureChannelId and/or TokenId are not currently in use. *)
  UA_STATUSCODE_BADTCPMESSAGETOOLARGE = $80800000; (* The size of the message specified in the header is too large. *)
  UA_STATUSCODE_BADTCPNOTENOUGHRESOURCES = $80810000; (* There are not enough resources to process the request. *)
  UA_STATUSCODE_BADTCPINTERNALERROR = $80820000; (* An internal error occurred. *)
  UA_STATUSCODE_BADTCPENDPOINTURLINVALID = $80830000; (* The server does not recognize the QueryString specified. *)
  UA_STATUSCODE_BADREQUESTINTERRUPTED = $80840000; (* The request could not be sent because of a network interruption. *)
  UA_STATUSCODE_BADREQUESTTIMEOUT = $80850000; (* Timeout occurred while processing the request. *)
  UA_STATUSCODE_BADSECURECHANNELCLOSED = $80860000; (* The secure channel has been closed. *)
  UA_STATUSCODE_BADSECURECHANNELTOKENUNKNOWN = $80870000; (* The token has expired or is not recognized. *)
  UA_STATUSCODE_BADSEQUENCENUMBERINVALID = $80880000; (* The sequence number is not valid. *)
  UA_STATUSCODE_BADPROTOCOLVERSIONUNSUPPORTED = $80BE0000; (* The applications do not have compatible protocol versions. *)
  UA_STATUSCODE_BADCONFIGURATIONERROR = $80890000; (* There is a problem with the configuration that affects the usefulness of the value. *)
  UA_STATUSCODE_BADNOTCONNECTED = $808A0000; (* The variable should receive its value from another variable *)
  UA_STATUSCODE_BADDEVICEFAILURE = $808B0000; (* There has been a failure in the device/data source that generates the value that has affected the value. *)
  UA_STATUSCODE_BADSENSORFAILURE = $808C0000; (* There has been a failure in the sensor from which the value is derived by the device/data source. *)
  UA_STATUSCODE_BADOUTOFSERVICE = $808D0000; (* The source of the data is not operational. *)
  UA_STATUSCODE_BADDEADBANDFILTERINVALID = $808E0000; (* The deadband filter is not valid. *)
  UA_STATUSCODE_UNCERTAINNOCOMMUNICATIONLASTUSABLEVALUE = $408F0000; (* Communication to the data source has failed. The variable value is the last value that had a good quality. *)
  UA_STATUSCODE_UNCERTAINLASTUSABLEVALUE = $40900000; (* Whatever was updating this value has stopped doing so. *)
  UA_STATUSCODE_UNCERTAINSUBSTITUTEVALUE = $40910000; (* The value is an operational value that was manually overwritten. *)
  UA_STATUSCODE_UNCERTAININITIALVALUE = $40920000; (* The value is an initial value for a variable that normally receives its value from another variable. *)
  UA_STATUSCODE_UNCERTAINSENSORNOTACCURATE = $40930000; (* The value is at one of the sensor limits. *)
  UA_STATUSCODE_UNCERTAINENGINEERINGUNITSEXCEEDED = $40940000; (* The value is outside of the range of values defined for this parameter. *)
  UA_STATUSCODE_UNCERTAINSUBNORMAL = $40950000; (* The value is derived from multiple sources and has less than the required number of Good sources. *)
  UA_STATUSCODE_GOODLOCALOVERRIDE = $00960000; (* The value has been overridden. *)
  UA_STATUSCODE_BADREFRESHINPROGRESS = $80970000; (* This Condition refresh failed *)
  UA_STATUSCODE_BADCONDITIONALREADYDISABLED = $80980000; (* This condition has already been disabled. *)
  UA_STATUSCODE_BADCONDITIONALREADYENABLED = $80CC0000; (* This condition has already been enabled. *)
  UA_STATUSCODE_BADCONDITIONDISABLED = $80990000; (* Property not available *)
  UA_STATUSCODE_BADEVENTIDUNKNOWN = $809A0000; (* The specified event id is not recognized. *)
  UA_STATUSCODE_BADEVENTNOTACKNOWLEDGEABLE = $80BB0000; (* The event cannot be acknowledged. *)
  UA_STATUSCODE_BADDIALOGNOTACTIVE = $80CD0000; (* The dialog condition is not active. *)
  UA_STATUSCODE_BADDIALOGRESPONSEINVALID = $80CE0000; (* The response is not valid for the dialog. *)
  UA_STATUSCODE_BADCONDITIONBRANCHALREADYACKED = $80CF0000; (* The condition branch has already been acknowledged. *)
  UA_STATUSCODE_BADCONDITIONBRANCHALREADYCONFIRMED = $80D00000; (* The condition branch has already been confirmed. *)
  UA_STATUSCODE_BADCONDITIONALREADYSHELVED = $80D10000; (* The condition has already been shelved. *)
  UA_STATUSCODE_BADCONDITIONNOTSHELVED = $80D20000; (* The condition is not currently shelved. *)
  UA_STATUSCODE_BADSHELVINGTIMEOUTOFRANGE = $80D30000; (* The shelving time not within an acceptable range. *)
  UA_STATUSCODE_BADNODATA = $809B0000; (* No data exists for the requested time range or event filter. *)
  UA_STATUSCODE_BADBOUNDNOTFOUND = $80D70000; (* No data found to provide upper or lower bound value. *)
  UA_STATUSCODE_BADBOUNDNOTSUPPORTED = $80D80000; (* The server cannot retrieve a bound for the variable. *)
  UA_STATUSCODE_BADDATALOST = $809D0000; (* Data is missing due to collection started/stopped/lost. *)
  UA_STATUSCODE_BADDATAUNAVAILABLE = $809E0000; (* Expected data is unavailable for the requested time range due to an un-mounted volume *)
  UA_STATUSCODE_BADENTRYEXISTS = $809F0000; (* The data or event was not successfully inserted because a matching entry exists. *)
  UA_STATUSCODE_BADNOENTRYEXISTS = $80A00000; (* The data or event was not successfully updated because no matching entry exists. *)
  UA_STATUSCODE_BADTIMESTAMPNOTSUPPORTED = $80A10000; (* The client requested history using a timestamp format the server does not support (i.e requested ServerTimestamp when server only supports SourceTimestamp). *)
  UA_STATUSCODE_GOODENTRYINSERTED = $00A20000; (* The data or event was successfully inserted into the historical database. *)
  UA_STATUSCODE_GOODENTRYREPLACED = $00A30000; (* The data or event field was successfully replaced in the historical database. *)
  UA_STATUSCODE_UNCERTAINDATASUBNORMAL = $40A40000; (* The value is derived from multiple values and has less than the required number of Good values. *)
  UA_STATUSCODE_GOODNODATA = $00A50000; (* No data exists for the requested time range or event filter. *)
  UA_STATUSCODE_GOODMOREDATA = $00A60000; (* The data or event field was successfully replaced in the historical database. *)
  UA_STATUSCODE_BADAGGREGATELISTMISMATCH = $80D40000; (* The requested number of Aggregates does not match the requested number of NodeIds. *)
  UA_STATUSCODE_BADAGGREGATENOTSUPPORTED = $80D50000; (* The requested Aggregate is not support by the server. *)
  UA_STATUSCODE_BADAGGREGATEINVALIDINPUTS = $80D60000; (* The aggregate value could not be derived due to invalid data inputs. *)
  UA_STATUSCODE_BADAGGREGATECONFIGURATIONREJECTED = $80DA0000; (* The aggregate configuration is not valid for specified node. *)
  UA_STATUSCODE_GOODDATAIGNORED = $00D90000; (* The request specifies fields which are not valid for the EventType or cannot be saved by the historian. *)
  UA_STATUSCODE_BADREQUESTNOTALLOWED = $80E40000; (* The request was rejected by the server because it did not meet the criteria set by the server. *)
  UA_STATUSCODE_BADREQUESTNOTCOMPLETE = $81130000; (* The request has not been processed by the server yet. *)
  UA_STATUSCODE_GOODEDITED = $00DC0000; (* The value does not come from the real source and has been edited by the server. *)
  UA_STATUSCODE_GOODPOSTACTIONFAILED = $00DD0000; (* There was an error in execution of these post-actions. *)
  UA_STATUSCODE_UNCERTAINDOMINANTVALUECHANGED = $40DE0000; (* The related EngineeringUnit has been changed but the Variable Value is still provided based on the previous unit. *)
  UA_STATUSCODE_GOODDEPENDENTVALUECHANGED = $00E00000; (* A dependent value has been changed but the change has not been applied to the device. *)
  UA_STATUSCODE_BADDOMINANTVALUECHANGED = $80E10000; (* The related EngineeringUnit has been changed but this change has not been applied to the device. The Variable Value is still dependent on the previous unit but its status is currently Bad. *)
  UA_STATUSCODE_UNCERTAINDEPENDENTVALUECHANGED = $40E20000; (* A dependent value has been changed but the change has not been applied to the device. The quality of the dominant variable is uncertain. *)
  UA_STATUSCODE_BADDEPENDENTVALUECHANGED = $80E30000; (* A dependent value has been changed but the change has not been applied to the device. The quality of the dominant variable is Bad. *)
  UA_STATUSCODE_GOODCOMMUNICATIONEVENT = $00A70000; (* The communication layer has raised an event. *)
  UA_STATUSCODE_GOODSHUTDOWNEVENT = $00A80000; (* The system is shutting down. *)
  UA_STATUSCODE_GOODCALLAGAIN = $00A90000; (* The operation is not finished and needs to be called again. *)
  UA_STATUSCODE_GOODNONCRITICALTIMEOUT = $00AA0000; (* A non-critical timeout occurred. *)
  UA_STATUSCODE_BADINVALIDARGUMENT = $80AB0000; (* One or more arguments are invalid. *)
  UA_STATUSCODE_BADCONNECTIONREJECTED = $80AC0000; (* Could not establish a network connection to remote server. *)
  UA_STATUSCODE_BADDISCONNECT = $80AD0000; (* The server has disconnected from the client. *)
  UA_STATUSCODE_BADCONNECTIONCLOSED = $80AE0000; (* The network connection has been closed. *)
  UA_STATUSCODE_BADINVALIDSTATE = $80AF0000; (* The operation cannot be completed because the object is closed *)
  UA_STATUSCODE_BADENDOFSTREAM = $80B00000; (* Cannot move beyond end of the stream. *)
  UA_STATUSCODE_BADNODATAAVAILABLE = $80B10000; (* No data is currently available for reading from a non-blocking stream. *)
  UA_STATUSCODE_BADWAITINGFORRESPONSE = $80B20000; (* The asynchronous operation is waiting for a response. *)
  UA_STATUSCODE_BADOPERATIONABANDONED = $80B30000; (* The asynchronous operation was abandoned by the caller. *)
  UA_STATUSCODE_BADEXPECTEDSTREAMTOBLOCK = $80B40000; (* The stream did not return all data requested (possibly because it is a non-blocking stream). *)
  UA_STATUSCODE_BADWOULDBLOCK = $80B50000; (* Non blocking behaviour is required and the operation would block. *)
  UA_STATUSCODE_BADSYNTAXERROR = $80B60000; (* A value had an invalid syntax. *)
  UA_STATUSCODE_BADMAXCONNECTIONSREACHED = $80B70000; (* The operation could not be finished because all available connections are in use. *)
